// Google Analytics 4 (GA4) Configuration\nconst gtag = function() {\n  dataLayer.push(arguments);\n};\n\ngtag('js', new Date());\n\ngtag('config', 'GA_MEASUREMENT_ID'); // Replace GA_MEASUREMENT_ID with your actual Tracking ID\n\n// Conversion tracking example\ngtag('event', 'conversion', {\n  'send_to': 'GA_MEASUREMENT_ID/conversion_label', // Replace GA_MEASUREMENT_ID and conversion_label with values from your GA settings\n  // Additional parameters can be added here\n});